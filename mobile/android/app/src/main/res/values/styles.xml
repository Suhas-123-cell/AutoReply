<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
  </style>
  <!-- Theme.App.SplashScreen removed: its parent Theme.SplashScreen came from
       expo-splashscreen, which no longer exists. Bootsplash setup is already
       deferred (needs a logo image) per the prior session's handoff notes,
       so the activity theme just points at AppTheme directly for now. -->
</resources>